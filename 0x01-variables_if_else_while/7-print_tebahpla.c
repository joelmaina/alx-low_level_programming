/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>

int main(void)
{
char mwisho= 'z';
char kikomo = '\n';
while (mwisho >= 'a')
{
putchar (mwisho);
mwisho--;
}
putchar (kikomo);
return (0);
}
