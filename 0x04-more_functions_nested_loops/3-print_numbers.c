#include "main.h"

/**
 * prints numbers from 1 to 9.
 *
 * Return: Always 0.
 */
void print_numbers(void);
{
	int a;
	int b = '\n';
	for(a = 48; a<=57; a++)
	{
	_putchar(a);
	}
	_putchar(b);
	return(0);
}
